<%= flash[:notice] if flash[:notice] %>
<%= flash[:error] if flash[:error]%>

<% if @entity[0].nil? %>
  <div class="empty">
    <h2>There's nothig to show</h2>
    <%= link_to "Create entity", new_entity_path, class: "btn btn__action" %>
  </div>
<% else %>
  <%= render "shared/entity" %>
<% end %>
